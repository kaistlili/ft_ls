
#include "ft_ls.h"

int main(int ac, char **av)
{
	t_dir_list *dir_list;
	char path[256];
	int i;
	
	i = 1;
/*	if (ac > 1)
	{
		if ((i = ft_parse_opt(av[i], option)) < 0)			return (-1);
		
		if (av[i] != NULL)
		{
			ft_handle_param(av + i, )
		}
	}
*/	 /*-1 error 1 no opts 2 job done */

}
